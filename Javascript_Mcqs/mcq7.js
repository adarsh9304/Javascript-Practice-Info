console.log(+true);
console.log(!'javascript');

// in js rest operator paramter will be not calculated as fun.length also whenever we assign default value to any paramater then after that it and after that all parameter will be become optional and it will not calulated as fun.length